{
  "name": "json-express",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r --parallel run dev",
    "test": "vitest run",
    "e2e": "pnpm --filter './example/**' test",
    "publish:all": "pnpm -r publish --access public"
  },
  "devDependencies": {
    "@json-express/core": "workspace:^",
    "@playwright/test": "^1.59.1",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^25.5.0",
    "tsdown": "^0.21.7",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "jiti": "^2.6.1",
    "js-yaml": "^4.1.1"
  }
}

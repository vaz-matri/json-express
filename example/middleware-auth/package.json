{
    "name": "middleware-auth",
    "version": "1.0.0",
    "description": "E2E test for middleware-auth — verifies JWT authentication",
    "scripts": {
        "serve": "json-express",
        "test": "playwright test"
    },
    "dependencies": {
        "@json-express/core": "workspace:*",
        "@json-express/cli": "workspace:*",
        "@json-express/middleware-auth": "workspace:*",
        "jsonwebtoken": "^9.0.2"
    }
}

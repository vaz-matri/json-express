{
    "name": "api-rest",
    "version": "1.0.0",
    "description": "E2E test for api-rest — verifies cross-collection search endpoint",
    "scripts": {
        "serve": "json-express",
        "test": "playwright test"
    },
    "dependencies": {
        "@json-express/core": "workspace:*",
        "@json-express/cli": "workspace:*"
    }
}

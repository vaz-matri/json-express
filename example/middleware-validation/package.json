{
    "name": "middleware-validation",
    "version": "1.0.0",
    "description": "E2E test for middleware-validation — verifies Zod schema rejection",
    "scripts": {
        "serve": "json-express",
        "test": "playwright test"
    },
    "dependencies": {
        "@json-express/boot": "workspace:*",
        "@json-express/middleware-validation": "workspace:*",
        "zod": "^3.22.4"
    }
}

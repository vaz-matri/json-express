{
    "name": "transport-fastify",
    "version": "1.0.0",
    "description": "E2E test for transport-fastify — verifies Fastify transport swap from Express",
    "scripts": {
        "serve": "json-express",
        "test": "playwright test"
    },
    "dependencies": {
        "@json-express/core": "workspace:*",
        "@json-express/cli": "workspace:*",
        "@json-express/transport-fastify": "workspace:*"
    }
}

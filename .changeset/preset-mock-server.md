---
"@json-express/preset-mock-server": minor
---

Introduced `@json-express/preset-mock-server`, a new dependency preset designed for zero-config local prototyping. This preset bundles the local JSON persistence adapter, the Faker mock data seeder, and the REST API generator into a single package, allowing developers to instantly spin up self-seeding E-commerce backends and mock servers with zero boilerplate.
